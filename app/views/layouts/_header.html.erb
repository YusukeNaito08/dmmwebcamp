<header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          
            <h4 class="nav-bar-brand" style="color:#fff;">Bookers02</h4>
          

          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown"
          aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="#navbarNavDropdown">
            <ul class="navbar-nav  ml-auto">
              <% if user_signed_in? %>
                <li>
                    <%=link_to user_path(current_user.id) do %>
                   <i class= "fas fa-home nav-link text-light">
                     <span >Home</span>
                      </i>
                   <% end %>
                </li>
                <li>
                    <%=link_to users_path do %>
                    <i class="fas fa-users nav-link text-light">
                     <span>Users</span>
                      </i>
                    <% end %>
                </li>
                <li>
                    <%= link_to  books_path do %>
                    <i class="fas fa-book-open nav-link text-light">
                     <span>Books</span>
                      </i>
                    <% end %>
                </li>
                <li>
                    <%= link_to  destroy_user_session_path, method: :delete  do %>
                    <i class="fas fa-sign-out-alt nav-link text-light">
                     <span>logout</span>
                      </i>
                    <% end %>
                </li>
            　 <% else %>
            　 <li>
                    <%=link_to root_path do %>
                   <i class= "fas fa-home nav-link text-light">
                     <span >Home</span>
                      </i>
                   <% end %>
                </li>
                <li>
                  <%=link_to homes_about_path do%>
                  <i class="fas fa-link nav-link text-light">
                    <span>About</span>
                  </i>
                  <% end %>
                </li>
                <li>
                   <%= link_to  new_user_registration_path do %>
                   <i class="fas fa-user-plus nav-link text-light">
                    <span>sign_up</span>
                   </i>
                   <% end %>
                </li>
                <li>
                  <%= link_to user_session_path do %>
                  <i class="fas fa-sign-in-alt nav-link text-light">
                    <span>sign_in</span>
                  </i>
                  <% end %>
                </li>
              <% end %>
             </ul>
          </div>
      </nav>
    </header>